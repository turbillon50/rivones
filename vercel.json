{
  "buildCommand": "PORT=3000 pnpm -r --filter @workspace/autospot run build && rm -rf public && (mv artifacts/autospot/dist/public public 2>/dev/null || mv dist/public public 2>/dev/null || (find . -type d -name 'public' -path '*/dist/*' -not -path '*/node_modules/*' -exec cp -r {} ./ \\; ))",
  "outputDirectory": "public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
