{
  "buildCommand": "PORT=3000 pnpm -r --filter @workspace/autospot run build",
  "outputDirectory": "artifacts/autospot/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
