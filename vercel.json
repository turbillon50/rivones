{
  "buildCommand": "PORT=3000 pnpm -r --filter @workspace/autospot run build && rm -rf public && (test -d artifacts/autospot/dist/public && mv artifacts/autospot/dist/public ./public || mv dist/public ./public)",
  "outputDirectory": "public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
