{
  "buildCommand": "VITE_OUT_DIR=/vercel/path0/public PORT=3000 pnpm -r --filter @workspace/autospot run build",
  "outputDirectory": "public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
